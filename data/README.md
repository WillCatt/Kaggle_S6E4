# Data Notes

- `train.csv`, `test.csv`, and `sample_submission.csv` are the Kaggle source files.
- Put cleaned or feature-engineered datasets in `data/processed/`.
- Avoid committing large derived files unless they are essential to reproduce results.

